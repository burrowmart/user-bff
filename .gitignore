dist/
node_modules/
*.js.map
*.d.ts.map

# Compiled artifacts that must NOT end up at the project root
# (nest build emits to dist/ only — these appear if tsc is run directly)
jest.config.js
jest.config.d.ts
.env
