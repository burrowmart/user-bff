/** Single source of truth for strings that change when cloning this template. */

/** Slug used for OTEL service.name, Helm release name, and image tag prefix. */
export const SERVICE_NAME = 'user-bff';
