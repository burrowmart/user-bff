# Server
PORT=3000
NODE_ENV=development

# Downstream services this BFF aggregates (REST, typed clients from @demo/contracts)
USER_SERVICE_URL=http://localhost:3001
NOTIFICATION_SERVICE_URL=http://localhost:3005

# Auth — Cognito
# Set AUTH_DISABLED=true locally when you don't have a Cognito pool
AUTH_DISABLED=false
# https://cognito-idp.{region}.amazonaws.com/{userPoolId}
COGNITO_ISSUER=
# App client ID (aud claim in the id_token)
COGNITO_AUDIENCE=

# Observability — OpenTelemetry
# Leave blank to use ConsoleSpanExporter (prints to stdout) for local dev
OTEL_EXPORTER_OTLP_ENDPOINT=
OTEL_SERVICE_NAME=user-bff
